# Calendar Sync

Replaces the AWS Lambda. Fetches the Nextcloud public calendar ICS, converts to paginated JSON, uploads to a public S3 bucket.

See [08-create-object-storage.md](../../docs/day-0/08-create-object-storage.md) for prerequisite secrets and bucket setup.
