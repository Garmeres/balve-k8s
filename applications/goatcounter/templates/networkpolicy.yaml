apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: goatcounter
spec:
  podSelector:
    matchLabels:
      app: goatcounter
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: traefik
      ports:
        - protocol: TCP
          port: 8080
  egress: []
